# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'DynamicView' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for DynamicView
  pod 'Masonry'
  pod 'SDWebImage' 
  pod 'SDWebImage/WebP'

  target 'DynamicViewTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'DynamicViewUITests' do
    # Pods for testing
  end

end
